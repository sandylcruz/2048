const Board = () => {
  const [grid, setGrid] = [];

  const generateGrid = () => {};
  // generate two numbers placed randomly on grid upon initialize of new game

  const currentTilePosition = () => {};
  const renderRows = () => {};
  const renderTiles = () => {};
};

export default Board;
